<?php
/**
 * Пример файла для запуска из консоли под управлением менеджера Convisor. См. документацию к движку, "Консоль".
 */

/**
 * Скрипт должен содержать эту функцию, без классовой обертки.
 * В параметры будет передано то, что вы укажете в вызове скрипта через Convisor.
 * @param $params
 */
function run($params) {
    dd($params);
}
